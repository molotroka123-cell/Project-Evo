# Силки `snare_line`

**Эпоха 0 — Каменный век** · требует технологию: `hunting`

---

## ИДЕЯ

Еда для тех, у кого нет рук. Один рабочий, копеечная цена — заполняет провал, когда население 5 человек и каждый на счету.

## ЛОГИКА

1.2 еды на одного рабочего = 1.2 на руку против 1.1 у собирателей (2.2/2) и 0.87 у стоянки (2.6/3). Лучшая отдача НА РУКУ, худшая суммарно. Это и есть выбор: много мелких или мало крупных.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
snare_line: { name: 'Силки', cost: { wood: 4 }, workers: 1, out: { food: 1.2 }, req: 'hunting' },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
snare_line: 0,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** Прямое добавление, новой логики нет.

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e0.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не давать больше 1.3: силки не должны обгонять стоянку по суммарной еде.

---

*Спецификация 07 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
