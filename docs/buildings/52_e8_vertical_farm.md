# Вертикальная ферма `vertical_farm`

**Эпоха 8 — Информационная** · требует технологию: `biotech`

---

## ИДЕЯ

Еда без земли. Снимает главное ограничение поздней игры — нехватку равнин под фермы — но требует стали и работает круглый год.

## ЛОГИКА

6.0 еды — вдвое больше фермы, БЕЗ needTile и зимой. Цена: 0.5 стали в день и высокая постройка.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
vertical_farm: { name: 'Вертикальная ферма', cost: { steel: 200, gold: 150 }, workers: 3, out: { food: 6.0 }, consume: { steel: 0.5 }, winter: true, req: 'biotech' },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
vertical_farm: 8,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** Прямое добавление.

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e8.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не убирать consume. Бесплатная еда без привязки к земле обрушит всю продовольственную экономику.

---

*Спецификация 52 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
