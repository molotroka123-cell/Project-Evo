# Медный рудник `copper_mine`

**Эпоха 1 — Бронзовый век** · требует технологию: `bronze`

---

## ИДЕЯ

Первая руда, дающая деньги. Делает горы ценными не только ради камня и разводит два типа гор: «на камень» и «на медь».

## ЛОГИКА

0.9 камня против 1.6 у шахты — заметно хуже, но +0.3 золота. Размен камня на деньги в чистом виде.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
copper_mine: { name: 'Медный рудник', cost: { wood: 20, stone: 10 }, workers: 3, out: { stone: 0.9, gold: 0.3 }, needTile: 6, req: 'bronze' },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
copper_mine: 1,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** Прямое добавление, needTile:6 как у mine.

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e1.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не делать её лучше шахты и по камню, и по золоту одновременно.

---

*Спецификация 14 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
