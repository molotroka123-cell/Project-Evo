# Арсенал `arsenal`

**Эпоха 5 — Ренессанс** · требует технологию: `gunpowder`

---

## ИДЕЯ

Военный квартал эпохи пороха: сильнейший множитель армии, но ест камень и усиливает соседние военные здания.

## ЛОГИКА

+25% армии — больше, чем у литейной (1.2), но −0.6 камня в день и требует военных соседей, чтобы аура не пропала зря.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
arsenal: { name: 'Арсенал', cost: { stone: 120, gold: 80 }, armyMult: 1.25, consume: { stone: 0.6 }, aura: { r: 3, army: 1.15 }, req: 'gunpowder' },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
arsenal: 5,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** Использует aura.army (см. плац).

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e5.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не давать работать при пустом складе камня.

---

*Спецификация 37 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
