# Дом старейшины `elder_hut`

**Эпоха 0 — Каменный век** · требует технологию: `language`

---

## ИДЕЯ

Три маленьких эффекта в одном здании. Показывает игроку, что здания бывают не «про одно», и готовит к многоцелевым зданиям поздних эпох.

## ЛОГИКА

0.35 знания против 0.5 у костра историй, но плюс жильё и счастье. Дороже на 2 дерева. Честный размен: узкая специализация против широты.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
elder_hut: { name: 'Дом старейшины', cost: { wood: 12 }, workers: 1, out: { knowledge: 0.35 }, happy: 2, housing: 2, req: 'language' },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
elder_hut: 0,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** Прямое добавление.

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e0.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не делать строго лучше костра историй — тогда костёр станет мёртвым контентом.

---

*Спецификация 08 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
