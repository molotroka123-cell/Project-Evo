# Ветролом `windbreak`

**Эпоха 0 — Каменный век** · требует технологию: `null`

---

## ИДЕЯ

Зима в игре бьёт по добыче. Ветролом — первый способ ЗАЩИТИТЬСЯ от погоды, а не терпеть её.

## ЛОГИКА

Снижает зимний штраф соседям на 20% в радиусе 3. Зимний множитель сейчас ×0.6 для зданий без winter:true — с ветроломом станет ×0.68. Скромно намеренно.

## ИСПОЛНЕНИЕ

Добавить в `app/src/core/data.js`, в объект `BUILDINGS`:

```js
windbreak: { name: 'Ветролом', cost: { wood: 10 }, aura: { r: 3, winter: 0.8 }, req: null },
```

И в `BUILDING_ERA_IDX` (или туда, где в этом дереве живёт привязка к эпохе):

```js
windbreak: 0,
```

Описание `desc` пиши сам, одной строкой, на русском, по правилу проекта:
говори игроку, ЧТО ЭТО ДАЁТ и КАКОЙ ЦЕНОЙ, а не пересказывай поля.

**Что править в коде:** aura.winter — новый ключ. Применять в produceAt там же, где считается сезонный множитель (искать "seasonIdx === 3").

**Тест.** Заведи проверку в существующем наборе по этой эпохе (или создай
`app/tests/test-buildings-e0.mjs`). Тест обязан:
1. падать на дереве БЕЗ этого здания и проходить с ним — проверь оба
   состояния сам, прежде чем сдавать;
2. проверять числа, а не факт существования ключа;
3. если у здания есть аура, отдельно проверять, что за пределами радиуса
   она НЕ действует.

## ЧЕГО НЕ ДЕЛАТЬ

Не превращать в отмену зимы. Зима обязана оставаться испытанием.

---

*Спецификация 10 из 58. Общие правила — в `docs/MASTER_PROMPT_FROM_DS.md`.*
